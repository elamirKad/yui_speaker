version: '3'
services:
  # server:
  #   build: .
  #   ports:
  #     - "8765:8765"
  #   volumes:
  #     - .:/usr/src/app
  #   environment:
  #     - NAME=World
  #   command: python checker.py
    
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"